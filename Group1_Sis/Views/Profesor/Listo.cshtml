@model List<Profesor>

<div class="container" >
    <div class="row p-3" >
        <div class="col-6" >
            <h2 class="text-primary">Lista e profesoreve</h2>
        </div>
        <div class="col-6 text-end" >
            <a class="btn btn-success" asp-controller="Profesor" asp-action="Krijo">Shto</a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Emri</th>
            <th>Mbiemri</th>
            <th>Komuna</th>
            <th>Veprimi</th>
        </tr>
        </thead>
        <tbody>
        @foreach (Profesor prof in Model)
        {
            <tr>
                    <td>@prof.Id</td>
                    <td>@prof.Emri</td>
                    <td>@prof.Mbiemri</td>
                    <td>@prof.Komuna.Emri</td>
                <td>
                    <div class="btn-group">
                        <a asp-controller="Profesor" asp-action="Ndrysho" asp-route-id="@prof.Id" class="btn btn-primary mx-1">Ndrysho</a>
                            <a asp-controller="Profesor" asp-action="Fshi" asp-route-id="@prof.Id" class="btn btn-danger mx-1">Fshi</a>
                    </div>
                </td>
            </tr>
        }
        
        </tbody>

       
    </table>
</div>