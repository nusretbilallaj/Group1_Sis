@model List<Student>

<div class="container" >
    <div class="row p-3" >
        <div class="col-6" >
            <h2 class="text-primary">Lista e studenteve</h2>
        </div>
        <div class="col-6 text-end" >
            <a class="btn btn-success" asp-controller="Student" asp-action="Krijo">Shto</a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Emri</th>
            <th>Mbiemri</th>
            <th>Komuna</th>
            <th>Veprimi</th>
        </tr>
        </thead>
        <tbody>
        @foreach (Student stud in Model)
        {
            <tr>
                <td>@stud.Id</td>
                    <td>@stud.Emri</td>
                    <td>@stud.Mbiemri</td>
                <td>@stud.Komuna.Emri</td>
                <td>
                    <div class="btn-group">
                        <a asp-controller="Student" asp-action="Ndrysho" asp-route-id="@stud.Id" class="btn btn-primary mx-1">Ndrysho</a>
                            <a asp-controller="Student" asp-action="Fshi" asp-route-id="@stud.Id" class="btn btn-danger mx-1">Fshi</a>
                    </div>
                </td>
            </tr>
        }
        
        </tbody>

       
    </table>
</div>